# Finance Flutter Prototype

Protótipo Flutter responsivo com backend mockado.

## Rodar localmente

```bash
cd flutter_prototype
flutter pub get
flutter run
```

## APK

```bash
flutter build apk --release
```

## Arquitetura

`MockFinanceApi` simula a API. Depois podemos trocar por chamadas HTTP para o backend .NET 10.
